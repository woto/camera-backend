<%= turbo_stream.replace "event-switcher-data" do %>
  <%= render "event_switcher_data" %>
<% end %>

<%= turbo_stream.replace "event-header" do %>
  <%= render "event_header" %>
<% end %>

<%= turbo_stream.replace "event-title" do %>
  <%= render "event_title" %>
<% end %>

<%= turbo_stream.replace "event-overlay" do %>
  <%= render "event_overlay" %>
<% end %>

<%= turbo_stream.replace "page-title" do %>
  <title id="page-title"><%= t("events.meta.title", id: @event.id) %></title>
<% end %>

<%= turbo_stream.replace "event-files" do %>
  <%= render "event_files" %>
<% end %>

<%= turbo_stream.replace "event-admin" do %>
  <%= render "event_admin" %>
<% end %>
